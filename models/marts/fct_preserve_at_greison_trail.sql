{{ config(enabled=false) }}
-- Stale model: original rawdata source no longer exists.
-- Replaced by the labeled pipeline: stg_preserve_at_greison_trail_labeled → int_labeled_greison_trail
select 1 as _placeholder
